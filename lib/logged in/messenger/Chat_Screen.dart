import 'package:flutter/material.dart';

class ChatScreen extends StatelessWidget {
  final String name;
  const ChatScreen({required this.name, super.key});
  @override
  Widget build(context) {
    return Scaffold(
      body: ListView(),
    );
  }
}
